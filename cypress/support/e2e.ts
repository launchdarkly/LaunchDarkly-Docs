import './commands'

// Cypress.on('uncaught:exception', (err, runnable) => {
//   // ignoring errors from initial render to hyradation changed content
//   const ignoredMessages = ["#418", "#422"]
//   if (ignoredMessages.some(msg => err.message.includes(msg))) {
//     return false
//   }
// })